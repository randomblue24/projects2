//mine class, which initilizes properties x, y coordinates for mine
//mines are randomly generated, and  accordign to a set of rules
class mine{
    constructor (x, y){
        this.x = x;
        this.y = y;
    }
}

class marks {
    constructor (x,y){
        this.x = x;
        this.y = y;
    }
}

//board, has two properties x and y.
//we also need to define how big the board is. We'll set it as 2 x 2
class board {
    constrctor (x, y){
        this.x = x;
        this. y = y;
    }
}